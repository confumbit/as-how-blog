<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>As How</title>
</head>
<body>
   <div class="header">
       <h1>As How</h1>
       <br>
       <h4><i>A blog where two people chronicle the sadness that ensues (their) life.</i></h4>
   </div>
   <h2 class="listTitle">Blog Posts</h2>
   <dl class="blogList">
        <%
        posts.forEach((post) => {
        %>

            <% let date = post.date.toLocaleDateString() %>
            <% let time = post.date.toLocaleTimeString() %>
            <a href=<%= "/blog/"+post._id %> >
                <dt><span><%= post.title %></span><span><%= date %>&nbsp;&nbsp;&nbsp;&nbsp;<%= time %></span></dt>
                <dd><%= post.subtitle %></dd>
            </a>
       <%
        });
       %>
   </dl>
</body>
</html>